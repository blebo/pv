pv - a Python-based PV inverter monitoring library

Change Log
==========

0.2 - 28 Jan 11
 * Added a new module to connect to pvoutput.org service.
 * Changed directory layout and moved pv into a subdirectory.
 * Removed all uses of @staticmethod.
 * Removed dependency on pyserial module. Instead, it expects an objects that
   behaves like pyserial.
 * Debug statements are now disabled by default and must be manually enabled.
 * ANSI colouring in debug statements are now disabled by default and must be
   manually enabled.

0.1 - 9 Jul 10
 * Initial alpha release
